@model PostUpdateViewModel
@using System.Security.Claims

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-12 col-md-10 col-lg-8">
            <div class="card shadow-sm border-0">
                <div class="card-body">
                    <h1 class="h4 mb-4 text-center">✏️ Post Düzenle</h1>

                    <form asp-action="Edit" method="post" novalidate>
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <input type="hidden" asp-for="PostId" />

                        <div class="mb-3">
                            <label asp-for="Title" class="form-label fw-semibold">Başlık</label>
                            <input asp-for="Title" class="form-control form-control-lg" placeholder="Başlığı güncelleyin" />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Description" class="form-label fw-semibold">Açıklama</label>
                            <input asp-for="Description" class="form-control form-control-lg" placeholder="Açıklamayı güncelleyin" />
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Url" class="form-label fw-semibold">URL</label>
                            <input asp-for="Url" class="form-control form-control-lg" placeholder="url-guncelle" />
                            <span asp-validation-for="Url" class="text-danger"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="Content" class="form-label fw-semibold">İçerik</label>
                            <textarea asp-for="Content" class="form-control" rows="6" placeholder="İçeriği güncelleyin..."></textarea>
                            <span asp-validation-for="Content" class="text-danger"></span>
                        </div>

                        @if (User.FindFirstValue(ClaimTypes.Role) == "admin")
                        {
                            <div class="form-check mb-4">
                                <input asp-for="IsActive" class="form-check-input" id="IsActive" />
                                <label class="form-check-label" for="IsActive">Yayında olsun</label>
                            </div>
                        }

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary btn-lg">💾 Güncelle</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
